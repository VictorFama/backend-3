# puerto en el que escucha la API
PORT=
# string de conexion de MongoDB Atlas
MONGODB_URI=
# development, production o test
NODE_ENV=
# nivel minimo que publica el logger: fatal, error, warning, info, http o debug, si se deja vacio: info en produccion, debug en el resto
LOG_LEVEL=